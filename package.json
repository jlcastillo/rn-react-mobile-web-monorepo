{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "cd web && npm run serve",
    "build": "cd common && npm install && cd ../web && npm install && npm run build && cd..",
    "test": "cd web && npm run test"
  },
  "engines": {
    "node": "8.9.4"
  }
}
